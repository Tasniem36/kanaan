# Copy to .env — used by docker-compose.
DB_NAME=dukkan
DB_PASSWORD=change-this-password
JWT_SECRET=generate-a-long-random-string

# Optional: auto-create the first manager account on startup.
SEED_MANAGER_EMAIL=admin@kanaan.ps
SEED_MANAGER_PASSWORD=change-this-too
