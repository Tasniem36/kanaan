fastapi==0.115.6
uvicorn[standard]==0.34.0
psycopg[binary]==3.2.3
psycopg-pool==3.2.4
PyJWT==2.10.1
bcrypt==4.2.1
requests==2.32.3
python-dotenv==1.0.1
Pillow==11.1.0
pywebpush
# Timezone database. python:*-slim carries none, so zoneinfo cannot resolve
# Asia/Dubai without it and every time the shop is shown falls back four hours.
tzdata
